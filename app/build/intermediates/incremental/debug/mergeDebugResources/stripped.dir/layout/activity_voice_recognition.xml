<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Start Voice Recognition Button -->
    <Button
        android:id="@+id/startVoiceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Voice Recognition"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"/>

    <!-- Stop Voice Recognition Button -->
    <Button
        android:id="@+id/stopVoiceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop Voice Recognition"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/startVoiceButton"
        android:layout_marginTop="20dp"/>

</RelativeLayout>
